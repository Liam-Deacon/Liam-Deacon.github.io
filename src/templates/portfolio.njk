<!-- Portfolio Section-->
<section class="portfolio" id="projects">
    <div style="padding-top: 200px;">
    <div class="container">
        <!-- Portfolio Section Heading-->
        <h1 class="headline text-center text-uppercase text-secondary mb-0">Portfolio</h1>
        
        <!-- Portfolio Grid Items-->
        <div class="row">
            {%- for project in data.portfolio.projects -%}
            <!-- Portfolio Item{{ loop.index }}: {{ project.name }} --> 
            <div class="col-md-6 col-lg-4 mb-5">
                <div class="portfolio-item mx-auto" data-toggle="modal" data-target="#portfolioModal{{ project.name | capitalize }}">
                    <div class="portfolio-item-caption d-flex align-items-center justify-content-center h-100 w-100">
                        <div class="portfolio-item-caption-content text-center text-white"><i class="fas fa-plus fa-3x"></i></div>
                    </div>
                    <img class="img-fluid" src="{{ project.image }}" alt="" />
                    <br>
                    <h4 href="{{ project.link }}" class="justify-content-center">{{ project.name }}</h4>
                </div>
            </div>
            {% endfor %}
        </div>
    </div> <!-- container -->
    </div>
</section> <!-- Portfolio -->